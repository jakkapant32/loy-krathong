# 🚀 คู่มือ Deploy บน Render.com

## ขั้นตอนการ Deploy

### 1. สร้างฐานข้อมูล PostgreSQL บน Render.com

1. เข้าไปที่ [Render Dashboard](https://dashboard.render.com)
2. คลิก **New +** → เลือก **PostgreSQL**
3. ตั้งค่าดังนี้:
   - **Name**: `loy-krathong-db` (หรือชื่อที่คุณต้องการ)
   - **Database**: `loy_krathong` (หรือปล่อยให้สุ่ม)
   - **User**: `loy_krathong_user` (หรือปล่อยให้สุ่ม)
   - **Region**: เลือกภูมิภาคที่ต้องการ (แนะนำ Oregon หรือ Singapore)
   - **PostgreSQL Version**: `17` (หรือเวอร์ชันล่าสุด)
4. คลิก **Create Database**
5. รอให้ฐานข้อมูลสร้างเสร็จ แล้วคัดลอก **Internal Database URL** หรือ **External Database URL**

### 2. ตั้งค่า Environment Variables

1. หลังจากสร้างฐานข้อมูลแล้ว ให้ไปที่หน้า Database
2. คัดลอก **Connection String** (Internal Database URL สำหรับใช้ใน Render, External Database URL สำหรับใช้จากนอก Render)
3. เก็บค่านี้ไว้สำหรับใช้ในขั้นตอนต่อไป

### 3. Deploy Web Service บน Render.com

#### วิธีที่ 1: Deploy จาก Git Repository (แนะนำ)

1. **Push โค้ดไปยัง Git Repository** (GitHub, GitLab, หรือ Bitbucket)
2. ใน Render Dashboard คลิก **New +** → เลือก **Web Service**
3. เชื่อมต่อ Git Repository ของคุณ
4. ตั้งค่าดังนี้:
   - **Name**: `loy-krathong`
   - **Environment**: `Node`
   - **Build Command**: `npm install && npx prisma generate && npm run build`
   - **Start Command**: `npm start`
   - **Node Version**: `20` (หรือเวอร์ชันล่าสุด)

5. **เพิ่ม Environment Variables**:
   - `DATABASE_URL`: วาง Connection String ที่คัดลอกไว้
   - `NODE_ENV`: `production`

6. คลิก **Create Web Service**

#### วิธีที่ 2: Deploy จาก `render.yaml`

1. ตรวจสอบว่าไฟล์ `render.yaml` อยู่ใน root directory
2. ใน Render Dashboard คลิก **New +** → เลือก **Blueprint**
3. เชื่อมต่อ Git Repository
4. Render จะสร้างทั้ง Database และ Web Service ให้อัตโนมัติ

### 4. รัน Database Migration

หลังจาก Deploy เสร็จแล้ว:

1. เข้าไปที่ Web Service ของคุณ
2. ไปที่แท็บ **Shell**
3. รันคำสั่ง:
   ```bash
   npx prisma migrate deploy
   ```
   หรือ
   ```bash
   npx prisma db push
   ```

### 5. ตรวจสอบการทำงาน

1. เปิดเว็บไซต์ที่ Render ให้ URL
2. ทดสอบการลอยกระทง
3. ตรวจสอบว่าข้อมูลถูกบันทึกลงฐานข้อมูลหรือไม่

## 🔧 การแก้ไขปัญหา

### ปัญหา: Database Connection Error

- ตรวจสอบว่า `DATABASE_URL` ถูกต้อง
- ใช้ **Internal Database URL** ถ้า Web Service อยู่ใน Render เดียวกัน
- ใช้ **External Database URL** ถ้าเชื่อมต่อจากภายนอก

### ปัญหา: Prisma Generate Error

- ตรวจสอบว่า `prisma generate` ถูกเรียกใน Build Command
- ตรวจสอบว่า `@prisma/client` อยู่ใน dependencies

### ปัญหา: Migration Error

- ตรวจสอบว่า schema ใน `prisma/schema.prisma` ถูกต้อง
- รัน `npx prisma migrate reset` ถ้าต้องการเริ่มใหม่ (⚠️ ระวัง: จะลบข้อมูลทั้งหมด)

## 📝 หมายเหตุ

- Render จะ restart service อัตโนมัติเมื่อมีการ push code ใหม่
- ตรวจสอบ logs ใน Render Dashboard เพื่อดู error messages
- สำหรับ production แนะนำให้ใช้ **Internal Database URL** เพื่อความปลอดภัย

## 🔗 ลิงก์ที่มีประโยชน์

- [Render Documentation](https://render.com/docs)
- [Prisma Deployment Guide](https://www.prisma.io/docs/guides/deployment/deployment-guides/deploying-to-render)

